<Window x:Class="CourseWork2.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseWork2"
        mc:Ignorable="d"
        Title="LoginForm" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="773*"/>
            <ColumnDefinition Width="27*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10,10,10,10" Background="AntiqueWhite" Grid.ColumnSpan="2"/>
        <StackPanel x:Name="stackPanel" Margin="10,10,0,350" Background="Bisque" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Label x:Name="LoginLabel" Content="Авторизация"
                   FontSize="{Binding ActualHeight, ElementName=LoginLabel, Mode=OneWay}"
                   FontFamily="Gabriola"
                   RenderTransformOrigin="0.8,0.514" 
                   HorizontalContentAlignment="Center" 
                   VerticalContentAlignment="Center" Height="75" Width="778"
            />
        </StackPanel>


    </Grid>
</Window>
