<Window x:Class="CourseWork2.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseWork2"
        mc:Ignorable="d"
        Title="LoginForm" Height="450" Width="800">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.25*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="backPanel" Background="AntiqueWhite" Grid.RowSpan="2" />
        <Label x:Name="loginLabel" Content="Авторизация" 
               HorizontalContentAlignment="Center" 
               VerticalContentAlignment="Center" 
               FontSize="50"
               Background="Bisque" Width="{Binding ActualWidth, ElementName=backPanel, Mode=OneWay}"
        />
    </Grid>
</Window>
